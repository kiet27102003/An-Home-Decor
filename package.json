{
  "name": "an-home-decor",
  "version": "1.0.0",
  "description": "Construction, Interior Design & Wine Selling Website",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "setup": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["construction", "interior-design", "wine"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

